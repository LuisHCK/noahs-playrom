-- Global runtime defaults used across core systems.
local config = {
    baseWidth = 1280,
    baseHeight = 720,
    defaultLanguage = "en",
    defaultAssetProfile = "prototype",
    deviceProfile = "default" -- "default" or "watch"
}

return config
