-- Global runtime defaults used across core systems.
local config = {
    baseWidth = 1280,
    baseHeight = 720,
    defaultLanguage = "en",
    defaultAssetProfile = "final",
    deviceProfile = "watch" -- "default" or "watch"
}

return config
